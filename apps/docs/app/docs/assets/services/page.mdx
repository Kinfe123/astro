---
title: "Services"
description: "Services explains the responsibility boundary around `assets/services`."
order: 260
---

# Services

Services explains the responsibility boundary around `assets/services`.
## What This Area Covers
Services explains the `assets/services` behavior that readers can rely on before they inspect supporting files.
## What It Actually Does
The available source comments do not describe this behavior directly, so this page stays conservative and points readers to the related source files below.

## Behavior Notes
- Recognizes package markers such as `type`.
- Normalizes inputs to values such as `type` -> `image/${targetFormat}`.
## How To Use This Page
- Start with the responsibility this area owns.
- Use the related setup, configuration, guide, or API pages when you need commands, options, or import paths.
- Treat the files below as verification points for maintainers, not as the primary reader experience.
## Typical Path
1. Start with the Services page to understand what this area is for.
2. Review the setup, configuration, or guide page when the behavior depends on runtime context.
3. Use API reference only when you need exact commands, imports, or options.
## Supporting Files
Use these files when you need to verify examples or update the behavior behind this page.

- `packages/astro/src/assets/services/noop.ts` - Noop application layer. It usually receives requests, coordinates business behavior, or connects domain services.
- `packages/astro/src/assets/services/service.ts` - Service application layer. It usually receives requests, coordinates business behavior, or connects domain services.
- `packages/astro/src/assets/services/sharp.ts` - Sharp application layer. It usually receives requests, coordinates business behavior, or connects domain services.
- `packages/astro/src/assets/services/squoosh.ts` - Squoosh application layer. It usually receives requests, coordinates business behavior, or connects domain services.
