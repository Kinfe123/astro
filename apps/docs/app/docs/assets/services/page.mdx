---
title: "Services"
description: "Services explains the `assets/services` area and the workflow it supports."
order: 260
---

# Services

Services explains the `assets/services` area and the workflow it supports.
## When To Use It
Services is a documented entrypoint in Astro. Use it when the workflow you are building matches this focused package surface instead of importing the whole package.
## Import Path
- `astro/assets/services/noop` - Choose this for the focused subpath rather than the main package entrypoint.
- `astro/assets/services/sharp` - Choose this for the focused subpath rather than the main package entrypoint.
- `astro/assets/services/squoosh` - Choose this for the focused subpath rather than the main package entrypoint.
## How It Fits
Services explains what this area is responsible for, how readers use it, and where to continue for setup or reference details.
## Typical Workflow
1. Start with the Services page to understand what this area is for.
2. Review the setup, configuration, or guide page when the behavior depends on runtime context.
3. Use API reference only when you need exact commands, imports, or options.
