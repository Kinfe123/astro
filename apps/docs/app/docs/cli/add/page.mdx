---
title: "Add"
description: "Add explains the responsibility boundary around `cli/add`."
order: 270
---

# Add

Add explains the responsibility boundary around `cli/add`.
## What This Area Covers
Add explains the `cli/add` behavior that readers can rely on before they inspect supporting files.
## What It Actually Does
The available source comments do not describe this behavior directly, so this page stays conservative and points readers to the related source files below.

## Behavior Notes
- Recognizes package markers such as `netlify`, `vercel`, `cloudflare`, `node`, `commandName`, `usage`, `integrationName`, `defaultConfigFile`.
- Normalizes inputs to values such as `netlify` -> `@astrojs/netlify`, `vercel` -> `@astrojs/vercel/serverless`, `cloudflare` -> `@astrojs/cloudflare`, `node` -> `@astrojs/node`, `commandName` -> `astro add`, `usage` -> `[...integrations] [...adapters]`, `integrationName` -> `Tailwind`, `defaultConfigFile` -> `./tailwind.config.mjs`, and more.
## How To Use This Page
- Start with the responsibility this area owns.
- Use the related setup, configuration, guide, or API pages when you need commands, options, or import paths.
- Treat the files below as verification points for maintainers, not as the primary reader experience.
## Typical Path
1. Start with the Add page to understand what this area is for.
2. Review the setup, configuration, or guide page when the behavior depends on runtime context.
3. Use API reference only when you need exact commands, imports, or options.
## Supporting Files
Use these files when you need to verify examples or update the behavior behind this page.

- `packages/astro/src/cli/add/imports.ts` - Repository file related to this page.
- `packages/astro/src/cli/add/wrapper.ts` - Repository file related to this page.
- `packages/astro/src/cli/add/babel.ts` - Repository file related to this page.
- `packages/astro/src/cli/add/index.ts` - Repository file related to this page.
