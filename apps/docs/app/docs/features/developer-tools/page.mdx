---
title: "Developer Tools"
description: "Developer tools help configure, inspect, and generate the pieces needed to run the product."
order: 40
---

# Developer Tools

Developer tools help configure, inspect, and generate the pieces needed to run the product.
## What This Is
This section explains the utilities that help developers inspect, configure, or generate parts of the product. These tools are not just UI screens; they are shortcuts for common setup, debugging, schema, email, or integration work.

The recommended way to install the latest version of Astro is by running the command below:
## When To Use This
- Use this page when you are operating, integrating, or changing developer tools.
- Read it before changing UI, routes, permissions, or data flows tied to this workflow.
- Pair it with setup and configuration pages when runtime values affect what the user sees.
## Core Concepts
- developer tools
- generators
- inspectors
- visual editors
- debug workflows
## Typical Workflow
1. Choose the tool that matches the setup or debugging task.
2. Review generated code or detected state before applying it to the host project.
3. Use tool output as a starting point, then validate it against the application's real configuration.
## In The Product
These are the product surfaces or existing docs sections readers are most likely to touch while using this workflow.

- README context: `install`.
## User Path
1. Choose the tool that matches the setup or debugging task.
2. Review generated code or detected state before applying it to the host project.
3. Use tool output as a starting point, then validate it against the application's real configuration.
## Practical Details
- CLI Usage: CLI usage explains the command-line mental model: which commands exist, when to use each command, which flags affect runtime behavior, and how the CLI discovers project configuration. Core concepts: commands, flags, config discovery, local development, standalone server.
- Services: Services explains what this area is responsible for, how readers use it, and where to continue for setup or reference details. Core concepts: Assets, baseService, BaseServiceTransform, ExternalImageService, ImageService, isLocalService, LocalImageService, LocalImageTransform.
- CLI Usage workflow: Choose the command that matches the task, such as starting the studio or initializing config.
- CLI Usage workflow: Install or run the CLI from the host project root.
- CLI Usage workflow: Pass flags for port, host, config path, watch mode, or other runtime behavior when defaults do not match the app.
## Expected Outcome
- You can describe the workflow in product terms before changing configuration or code.
- You know which permissions, data, or runtime settings can change the user's experience.
- You know where to continue when you need exact commands, imports, or deployment details.
## From The README
The README already describes this area in project language. Use these excerpts as the reader-facing baseline before adding more examples.

### Install
The recommended way to install the latest version of Astro is by running the command below:
## Supporting Files
Use these files when you need to verify examples or update the behavior behind this page.

- `packages/astro/src/cli/add/babel.ts` - Repository file related to this page.
- `packages/astro/src/cli/add/imports.ts` - Repository file related to this page.
- `packages/astro/src/cli/add/index.ts` - Repository file related to this page.
- `packages/astro/src/cli/add/wrapper.ts` - Repository file related to this page.
- `packages/astro/src/cli/check/index.ts` - Repository file related to this page.
- `packages/astro/src/cli/db/index.ts` - Repository file related to this page.
