---
title: "Client Directive"
description: "Client Directive explains the responsibility boundary around `core/client-directive`."
order: 400
---

# Client Directive

Client Directive explains the responsibility boundary around `core/client-directive`.
## What This Area Covers
Client Directive explains the `core/client-directive` behavior that readers can rely on before they inspect supporting files.
## What It Actually Does
- `buildClientDirectiveEntrypoint(name: string, entrypoint: string, root: URL)` - Build a client directive entrypoint into code that can directly run in a `<script>` tag.

## Behavior Notes
- Recognizes package markers such as `format`.
- Normalizes inputs to values such as `format` -> `iife`.
## How To Use This Page
- Start with the responsibility this area owns.
- Use the related setup, configuration, guide, or API pages when you need commands, options, or import paths.
- Treat the files below as verification points for maintainers, not as the primary reader experience.
## Typical Path
1. Start with the Client Directive page to understand what this area is for.
2. Review the setup, configuration, or guide page when the behavior depends on runtime context.
3. Use API reference only when you need exact commands, imports, or options.
## Supporting Files
Use these files when you need to verify examples or update the behavior behind this page.

- `packages/astro/src/core/client-directive/default.ts` - Repository file related to this page.
- `packages/astro/src/core/client-directive/build.ts` - Repository file related to this page.
- `packages/astro/src/core/client-directive/index.ts` - Repository file related to this page.
