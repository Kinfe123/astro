---
title: "Runtime"
description: "Runtime explains the `content/runtime` area and the workflow it supports."
order: 310
---

# Runtime

Runtime explains the `content/runtime` area and the workflow it supports.
## When To Use It
Runtime is a documented entrypoint in Astro. Use it when the workflow you are building matches this focused package surface instead of importing the whole package.
## Import Path
- `astro/content/runtime` - Choose this for the focused subpath rather than the main package entrypoint.
## How It Fits
Runtime explains what this area is responsible for, how readers use it, and where to continue for setup or reference details.
## Typical Workflow
1. Start with the Runtime page to understand what this area is for.
2. Review the setup, configuration, or guide page when the behavior depends on runtime context.
3. Use API reference only when you need exact commands, imports, or options.
