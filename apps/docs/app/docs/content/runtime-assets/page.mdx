---
title: "Runtime Assets"
description: "Runtime Assets explains the `content/runtime-assets` area and the workflow it supports."
order: 300
---

# Runtime Assets

Runtime Assets explains the `content/runtime-assets` area and the workflow it supports.
## When To Use It
Runtime Assets is a documented entrypoint in Astro. Use it when the workflow you are building matches this focused package surface instead of importing the whole package.
## Import Path
- `astro/content/runtime-assets` - Choose this for the focused subpath rather than the main package entrypoint.
## How It Fits
Runtime Assets explains what this area is responsible for, how readers use it, and where to continue for setup or reference details.
## Typical Workflow
1. Start with the Runtime Assets page to understand what this area is for.
2. Review the setup, configuration, or guide page when the behavior depends on runtime context.
3. Use API reference only when you need exact commands, imports, or options.
